#include "List.h"

#include <stdio.h>
#include <stdlib.h>

int main (void){
	//I had a create_node funtion I used to test the list, however this isn't apart of prog3 so I cut it out;
	/*node_t *node = create_node({0,1,2,3,4}, 'a');
	node_t *nodetwo = create_node({1,2,3,4,5}, 'b');
	insertList(node,nodetwo);
	printList(*findList(node,nodetwo));
	deleteList(node,nodetwo);*/
	 
	
	return 1;

}
